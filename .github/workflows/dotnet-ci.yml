name: .NET CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: |
          dotnet restore GitHubActionsDemo.csproj
          dotnet restore GitHubActionsDemo.Tests/GitHubActionsDemo.Tests.csproj

      - name: Build
        run: dotnet build GitHubActionsDemo.csproj --no-restore --configuration Release

      - name: Run tests
        run: dotnet test GitHubActionsDemo.Tests/GitHubActionsDemo.Tests.csproj --no-restore --configuration Release